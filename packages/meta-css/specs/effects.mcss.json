{
	"templates": [
		{
			"name": "blur",
			"doc": { "group": "fx", "args": ["radius: incl. units"] },
			"props": {
				"filter": "blur({0})"
			}
		},
		{
			"name": "bg-blur",
			"doc": { "group": "fx", "args": ["radius: incl. units"] },
			"props": {
				"backdrop-filter": "blur({0})",
				"-webkit-backdrop-filter": "blur({0})"
			}
		}
	]
}
